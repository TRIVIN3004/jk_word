{
  "name": "jk-interactive-docx-platform",
  "version": "1.0.0",
  "description": "Interactive Document Viewer & Editor with Dashboard Navigation",
  "scripts": {
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "dev": "concurrently \"npm run dev --prefix client\" \"npm start --prefix server\"",
    "start": "concurrently \"npm run build --prefix client && npm start --prefix client\" \"npm start --prefix server\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
